public class Quiz {
    public static void main(String[] args){
        /*
        4-1)
        1. 10 < x && x < 20
        2. ch == '' && ch!= '\t'
        3. ch = 'x' || ch = 'X'
        4. '0' <= ch && ch <= '9'
        5. 'a' <= ch && ch <= 'Z' / 65 <= ch && ch <= 97
        6. (year%400==0 || year%4==0) && year%100!=0
        7. powerOn == false
        8. str == "yes"

        4-2)
        int sum =0;
        for(int i = 1 ; i <= 20 ; i++){
            if(i%2== 0 || i%3 == 0) sum+=i;
        }

        4-3)
        int sum, tmp =0;
        for(int i = 0 ; i < 10; i++){
            tmp = i+1;
            sum += i + tmp;
        }

        4-4)
        int sum, i = 0;
        while(sum<=100){

        }
         */
    }
}
